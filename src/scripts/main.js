/**
 * AUTOINTRO: src/scripts/main.js
 * Objetivo: Ponto de entrada do frontend que inicializa a aplicacao no navegador.
 * Escopo: Define a responsabilidade completa deste modulo dentro da arquitetura do projeto.
 */
import { iniciarAplicacao } from "./core/app.js";

iniciarAplicacao();
